name: Build Android Fixed Speed

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Free Disk Space
      uses: jlumbroso/free-disk-space@main
      # ... (Các cấu hình tối ưu hóa không gian)
    - name: Install Build Tools
      run: |
        sudo apt update
        # ... (Lệnh cài đặt build tools)
    - name: Setup Repo Tool
      run: |
        # ... (Thiết lập Git Repo)
    - name: Download Android Source (LineageOS 20)
      run: |
        # ... (Tải source LineageOS 20.0 cho Nokia 6.1)
    - name: Compile OS
      run: |
        # ... (Lệnh biên dịch mka bacon)
    - name: Upload ROM Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Nokia-6.1-Android-ZIP
        path: ~/android/out/target/product/PL2/*.zip
